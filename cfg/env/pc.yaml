name: position_control

dynamics: ${dynamics}
dt: 0.0333
length: 10
n_envs: ${n_envs}
n_agents: ${n_agents}
max_time: 20
wait_before_truncate: 5.
max_target_vel: 10.
min_target_vel: 5.

solver_type: 
n_substeps: 1

defaults:
  - render: pc_render
  - _self_